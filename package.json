{
  "name": "roller-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "format": "oxfmt",
    "knip": "knip",
    "lint": "pnpm -r lint",
    "prepare": "husky"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@types/node": "^24",
    "husky": "^9.1.7",
    "knip": "^6.14.1",
    "lint-staged": "^17.0.5",
    "oxfmt": "^0.59.0"
  },
  "lint-staged": {
    "*": "oxfmt --no-error-on-unmatched-pattern"
  }
}
