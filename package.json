{
  "name": "roller-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "format": "oxfmt",
    "knip": "knip",
    "lint": "pnpm -r lint",
    "prepare": "husky"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@types/node": "^24",
    "husky": "^9.1.7",
    "knip": "^6.27.0",
    "lint-staged": "^17.0.8",
    "oxfmt": "^0.59.0"
  },
  "lint-staged": {
    "*": "oxfmt --no-error-on-unmatched-pattern"
  }
}
