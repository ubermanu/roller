{
  "name": "roller-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "format": "oxfmt",
    "fallow": "fallow dead-code",
    "lint": "pnpm -r lint",
    "prepare": "husky"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@types/node": "^24",
    "fallow": "^3.6.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8",
    "oxfmt": "^0.59.0"
  },
  "lint-staged": {
    "*": "oxfmt --no-error-on-unmatched-pattern"
  }
}
